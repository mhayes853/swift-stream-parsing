// Intentionally empty: the module is header-only. SwiftPM needs one source file to build a
// C target, and the header's `static inline` functions are what the Swift side inlines.
#include "StreamParsingShims.h"
